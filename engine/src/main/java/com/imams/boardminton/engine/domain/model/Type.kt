package com.imams.boardminton.engine.domain.model

enum class MatchType {
    Single, Double
}

enum class GameType {
    One, Two, Three
}

enum class Side {
    A, B
}

enum class OnServe {
    A, B, NONE
}

enum class Server {
    P1, P2, NONE
}

enum class PlayerType {
    MS, WS, MD, WD, XD
}